[package]
name = "ipxml-cc"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
ipxml-schema = { path = "../ipxml-schema" }
ipxml-bundle = { path = "../ipxml-bundle" }
anyhow = "1.0.102"
